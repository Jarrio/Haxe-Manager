package;

typedef Flixel = {
    var name:String;
    var height:Int;
    var width:Int;
}

typedef Structure = {
    var files:Array<String>;
    var directories:Array<String>;
}

typedef ClassTemplate = {
    var type:String;
    var description:String;
}